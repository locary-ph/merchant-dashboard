import React from 'react';
import PropTypes from 'prop-types';

function ShopSettings({  }) {
  return (
    <div />
  );
}

ShopSettings.defaultProps = {};

ShopSettings.propTypes = {
};

export default ShopSettings;
